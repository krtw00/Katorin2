<% if record.errors.any? %>
  <div class="panel panel--danger">
    <strong><%= t("shared.errors.title") %></strong>
    <ul class="compact-list">
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
